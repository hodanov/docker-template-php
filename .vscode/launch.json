{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "port": 10903,
            "pathMappings": {
                 "/usr/src/app": "${workspaceFolder}/php/app"
             }
        },
    ]
}
